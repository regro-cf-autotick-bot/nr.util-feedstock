{% set name = "nr.util" %}
{% set version = "0.8.8" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/nr.util-{{ version }}.tar.gz
  sha256: 4d33640b2f6d93eedeebc2cc781aab141bf5b8ecfc649d6e349175cbcbde516c

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - poetry-core >=1.0.0
    - python >=3.7
  run:
    - deprecated >=1.2.0,<2.0.0
    - python >=3.7
    - typing-extensions >=3.0.0

test:
  imports:
    - nr.util
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/NiklasRosenstein/python-nr.util
  summary: General purpose Python utility library.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - NiklasRosenstein
